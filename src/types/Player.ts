export type Player = {
    id: number
    name: String
    cash: number
    inJail: Boolean
    pin: number
    isIA: Boolean
    plays: number
    next: Boolean
    square: number
    orderDice?: number
    properties: number[]
}
